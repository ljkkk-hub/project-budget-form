# 2026-05-18

## 项目立项预算表网页工具
- 基于用户提供的 Excel 模板（项目预算表格 副本6.xlsx），创建了单 HTML 文件的网页填写工具
- 功能：主管在线填写立项金额和 KPI 数据，一键导出与模板格式一致的 Excel 文件
- 技术栈：纯前端（HTML + Tailwind CSS + SheetJS），无需后端
- 包含两个核心区域：KPI 指标表（支持动态增删行）和成本预算表（6类成本×12月）
- 实时计算和校验（权重合计=100%、金额合计=合同收入）
- Excel 导出还原模板格式：合并单元格、边框、列宽、公式、黄色填充
- 文件位置：`C:\Users\admin\WorkBuddy\2026-05-18-task-16\index.html`

### 修复成本表不显示问题
- 根因：`initKpi()` → `recalcAll()` → `recalcCost()` 时成本 DOM 还不存在，`getElementById` 返回 null 导致 JS 报错中断，`initCost()` 未执行
- 修复：给 `recalcCost()` 加了 null 安全检查

### 新增立项毛利率功能
- 基本信息区新增"立项毛利率"输入框（3列布局）
- 计算公式：实际成本 = 合同金额 ÷ 1.06 × (1 - 毛利率)，其中 1.06 为 6% 增值税
- 顶部蓝色信息栏实时展示实际成本，成本表底部橙色"实际成本"行做对比
- 自动校验：成本合计 vs 实际成本，超出/不足金额标红提示
- Excel 导出在成本合计行下方新增实际成本行，备注列写明毛利率

### 新增项目日期和一级产品选项
- 隐藏成本预算表 number input 的上下箭头（CSS `-webkit-appearance: none`）
- 基本信息区扩展为5列布局
- 日期拆分为"项目开始日期"和"项目截止日期"两个 date input
- 一级产品选项（13项）：图文平台代运营、视频平台代运营、图文内容制作、视频内容制作、内容营销矩阵、品效增长、传播、直播、海外代运营、其他、洞察力、影响力、视频代运营
- 毛利率、实际成本条、文件命名预览放在第二行

### 新增项目预算表格命名预览
- 只读字段，自动生成命名：一级产品-项目名称-开始月份-截止月份项目预算表格
- 日期格式化为 yy.MM（如 26.05）
- 示例：视频内容制作-万镜一刻发布会宣传视频-26.05-26.05项目预算表格
- 填写信息后预览区变绿色背景，未填则灰色
- `getFileName()` 函数统一生成命名，`generateExcel()` 使用此命名作为文件名

### 布局重构与 Excel 修复（v3）
- 页面布局改为三行结构：
  - 第一行（4列）：一级产品 | 项目名称 | 开始日期 | 截止日期
  - 第二行（3列）：毛利率 | 合同金额 | 实际成本条（跨2列，更大更醒目）
  - 第三行（整行）：命名预览
- 移除成本表底部的"实际成本"对比行，避免页面冗余
- Excel 生成修复：
  - **移除实际成本单独行**：不再在 Excel 第10行插入实际成本，保持原始模板结构
  - **修复循环引用**：合计行 SUM 公式上限改为 `lastDataR0`（0索引），对应 Excel 第 `lastDataR0` 行（padding 最后一行），不包含合计行自己
  - **修正 padding 逻辑**：`lastDataR0 = Math.max(kpiRows.length + 1, 19)`，padding 从 `kpiRows.length+1` 到 `lastDataR0-1`
  - **全局加边框**：遍历 `ws` 所有单元格，给每个 cell 加 `s.border`（thin black），生成带框线的 Excel
